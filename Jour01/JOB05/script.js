function afficherJoursSemaines() {
    // Tableau des jours
    let joursSemaines = ["Lundi", "Mardi", "Mercredi", "Jeudi", "Vendredi", "Samedi", "Dimanche"];

    // Boucle pour afficher les jours de la semaine
    for (let i = 0; i < joursSemaines.length; i++) {
        console.log(joursSemaines[i]); // Affiche chaque jour de la semaine
    }
}

// Affichage des résultats
afficherJoursSemaines();
